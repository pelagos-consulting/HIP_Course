# (PMC1) Cache hits and Cache misses
pmc: TCC_HIT_sum, TCC_MISS_sum

# (PMC2) Total video memory fetched
pmc: FETCH_SIZE
# (PMC3) Total video memory written
pmc: WRITE_SIZE

# (PMC4) Percentage of time the GPU was busy, total wavefronts executed
pmc: GPUBusy, Wavefronts

# (PMC5) Average number of vector and scalar instructions executed per work-item
pmc: VALUInsts, SALUInsts

# (PMC6) Average number of vector and scalar fetch instructions per work-item
pmc: VFetchInsts, SFetchInsts

# (PMC7) Average number of vector write instructions per work-item
pmc: VWriteInsts

# (PMC8) Average number of shared and global memory read or write instructions per work item
pmc: LDSInsts, GDSInsts

# (PMC9) Percentage of active vector ALU threads in a wave, percentage of GPU time vector and scalar instructions are processed
pmc: VALUUtilization, VALUBusy, SALUBusy, 

# (PMC10) Percentage of fetch, write, atomic, and other instructions that hit the L2 cache
pmc: L2CacheHit

# (PMC11) Percentage of time the memory unit is active (including stalled), and just stalled, percentage of time the write unit is stalled
pmc: MemUnitBusy, MemUnitStalled, WriteUnitStalled

# (PMC12) Percentage of time ALU's are stalled by shared memory access, percentage of GPU time local memory is stalled by bank conflicts
pmc: ALUStalledByLDS, LDSBankConflict

# Dispatches range, which kernel launches from CPU to GPU do we actually profile?
# In this code we only launch the matrix multiplication kernel once.
range: 0 : 0
# Which GPU's to profile (use spaces to separate)
gpu: 0
# Names of kernels to profile (use spaces to separate)
kernel: mat_mult
